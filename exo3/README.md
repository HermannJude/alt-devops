# Issues fixed

- Added postgres healthcheck to ensure Mattermost starts only when the database is ready
- Added use of `.env` file in `compose.yml`
- Specify ssl mode to allow Mattermost container to start
