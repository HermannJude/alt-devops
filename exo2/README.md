# Issues fixed

- Upgraded PostgresSQL image to a more recent version
- Set healtcheck for PostgresSQL to ensure Nextcloud starts only when the database is ready
- Added use of `.env` file in `compose.yml`
